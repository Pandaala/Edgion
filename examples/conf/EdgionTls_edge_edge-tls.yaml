apiVersion: edgion.io/v1
kind: EdgionTls
metadata:
  name: edge-tls
  namespace: edge
spec:
  parentRefs:
    - name: example-gateway
      namespace: edge
  hosts:
    - test.example.com
    - grpc.example.com
  secretRef:
    name: edge-tls
    namespace: edge


apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: test-udp
  namespace: edge
spec:
  parentRefs:
    - name: example-gateway
      namespace: edge
      sectionName: udp
      port: 19002
  rules:
    - backendRefs:
        - name: test-udp
          port: 30011
          weight: 100


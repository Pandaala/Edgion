# HTTP Filters shared backend service (k8s selector mode)
apiVersion: v1
kind: Service
metadata:
  name: test-http
  namespace: edgion-test
spec:
  selector:
    app: edgion-test-server
  ports:
    - name: http-30001
      port: 30001
      protocol: TCP
      targetPort: 30001

# HTTP Filters shared Gateway for HeaderModifier suite
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: filters-test-gateway
  namespace: edgion-test
spec:
  gatewayClassName: public-gateway
  listeners:
    - name: http
      protocol: HTTP
      port: 31045

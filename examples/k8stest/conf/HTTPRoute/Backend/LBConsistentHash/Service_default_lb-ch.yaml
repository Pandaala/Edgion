# LB ConsistentHash （ slice / EP ）
apiVersion: v1
kind: Service
metadata:
  name: lb-ch-service
  namespace: edgion-default
spec:
  selector:
    app: edgion-test-server
  ports:
    - port: 8080
      protocol: TCP

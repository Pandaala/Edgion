# Timeout 测试 Service
apiVersion: v1
kind: Service
metadata:
  name: test-http
  namespace: edgion-test
spec:
  selector:
    app: edgion-test-server
  ports:
    - port: 30001
      targetPort: 30001
      protocol: TCP

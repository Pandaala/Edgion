apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jwe-decrypt-test-route
  namespace: edgion-default
spec:
  parentRefs:
    - name: edgion-plugins-gateway
      namespace: edgion-test
  hostnames:
    - "jwe-decrypt-test.example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: edgion.io
            kind: EdgionPlugins
            name: jwe-decrypt-test
      backendRefs:
        - name: test-http
          namespace: edgion-test
          port: 30001


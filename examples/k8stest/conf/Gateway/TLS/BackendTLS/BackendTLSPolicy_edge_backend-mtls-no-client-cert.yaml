apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: backend-mtls-no-client-cert
  namespace: edgion-test
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: test-backend-mtls-no-client-cert
  validation:
    caCertificateRefs:
      - name: backend-ca
        kind: Secret
        group: ""
    hostname: backend.example.com

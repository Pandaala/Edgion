# EdgionStreamPlugins: allow only localhost (K8s)
apiVersion: edgion.io/v1
kind: EdgionStreamPlugins
metadata:
  name: tls-allow-localhost
  namespace: edgion-test
spec:
  plugins:
    - type: IpRestriction
      config:
        defaultAction: deny
        allowList:
          - "127.0.0.0/8"

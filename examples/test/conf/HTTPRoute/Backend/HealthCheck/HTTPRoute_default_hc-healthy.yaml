apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hc-healthy-route
  namespace: edgion-default
spec:
  parentRefs:
    - name: hc-gateway
      namespace: edgion-test
  hostnames:
    - "hc-healthy.example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: hc-healthy
          port: 30001

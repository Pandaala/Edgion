apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hc-unhealthy-route
  namespace: edgion-default
spec:
  parentRefs:
    - name: hc-gateway
      namespace: edgion-test
  hostnames:
    - "hc-unhealthy.example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: hc-unhealthy
          port: 30001

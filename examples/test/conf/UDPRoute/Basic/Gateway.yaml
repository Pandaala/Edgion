# UDP  Gateway
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: udp-test-gateway
  namespace: edgion-test
spec:
  gatewayClassName: public-gateway
  listeners:
    - name: udp
      protocol: UDP
      port: 31100

apiVersion: edgion.io/v1
kind: EdgionStreamPlugins
metadata:
  name: leak-stream-plugins
  namespace: edgion-test
spec:
  plugins:
    - type: IpRestriction
      config:
        defaultAction: allow

apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: leak-btls
  namespace: edgion-test
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: leak-svc
  validation:
    caCertificateRefs:
      - name: edge-tls
        kind: Secret
        group: ""
    hostname: leak-svc.edgion-test.svc.cluster.local

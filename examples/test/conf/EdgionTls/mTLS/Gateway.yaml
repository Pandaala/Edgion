# mTLS 测试套件 Gateway
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: mtls-test-gateway
  namespace: edgion-test
spec:
  gatewayClassName: public-gateway
  listeners:
    # mTLS 监听器 - 由 EdgionTls 配置 TLS
    - name: https-mtls
      protocol: HTTPS
      port: 31110

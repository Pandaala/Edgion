# 停止所有后台进程
./tests/kill_all.sh

# 使用完整参数名 --config-file
cargo run --bin edgion-controller -- --config-file ./config/edgion-controller.toml
cargo run --bin edgion-gateway -- --config-file ./config/edgion-gateway.toml

# 或使用短参数 -c
cargo run --bin edgion-controller -- -c ./config/edgion-controller.toml
cargo run --bin edgion-gateway -- -c ./config/edgion-gateway.toml





################# test websocket #################
cargo run --example test_websocket_server
# client测试直连
cargo run --example test_websocket_client http://127.0.0.1:30011/ws ws.example.com

# curl测试
curl -i -N -H "Connection: Upgrade" \
     -H "Upgrade: websocket" \
     -H "Host: ws.example.com" \
     -H "Sec-WebSocket-Version: 13" \
     -H "Sec-WebSocket-Key: SGVsbG8sIHdvcmxkIQ==" \
     http://127.0.0.1:18080/ws

# client测试连网关
cargo run --example test_websocket_client http://127.0.0.1:18080/ws ws.example.com




############## test tcp #############
# start test-server
cargo run -p edgion --example test_http_server

./test_tcp_client.sh




################# test grpc #################
# 1. 启动 gRPC 测试服务器（自动启动4个端口: 30021-30024）
cargo run --example test_grpc_server

# 2. 客户端测试 - 测试 Server 1 (30021)
cargo run --example test_grpc_client http://127.0.0.1:30021

# 3. 客户端测试 - 测试 Server 2 (30023)
cargo run --example test_grpc_client http://127.0.0.1:30023

# 4. 使用 grpcurl 直连测试（需要安装 grpcurl）
# 安装: brew install grpcurl
# SayHello 方法测试
grpcurl -plaintext \
  -proto examples/proto/test_service.proto \
  -d '{"name":"World"}' \
  127.0.0.1:30021 \
  test.TestService/SayHello

# StreamNumbers 方法测试（流式）
grpcurl -plaintext \
  -proto examples/proto/test_service.proto \
  -d '{"count":5}' \
  127.0.0.1:30021 \
  test.TestService/StreamNumbers

# 5. 通过 Gateway 测试（需要完整环境）
# 注意: Gateway 需要 Controller 提供配置（包括 GRPCRoute）
#
# 完整启动步骤:
# Terminal 1: 启动 gRPC 测试服务器
# cargo run --example test_grpc_server
#
# Terminal 2: 启动 controller（关键！提供配置）
# cargo run --bin edgion-controller -- -c ./config/edgion-controller.toml
#
# Terminal 3: 启动 gateway
# cargo run --bin edgion-gateway -- -c ./config/edgion-gateway.toml
# (等待日志显示 "GRPCRoute loaded" 等配置加载信息)
#
# Terminal 4: 通过 Gateway 测试 HTTP/2 (h2c) - 端口 18080
grpcurl -plaintext \
  -proto examples/proto/test_service.proto \
  -H "Host: grpc.example.com" \
  -d '{"name":"World"}' \
  127.0.0.1:18080 \
  test.TestService/SayHello

# 或通过 HTTPS（端口 18443）
# grpcurl -insecure \
#   -proto examples/proto/test_service.proto \
#   -H "Host: grpc.example.com" \
#   -d '{"name":"World"}' \
#   127.0.0.1:18443 \
#   test.TestService/SayHello

# 6. 负载均衡测试（多次请求，观察分配到不同后端）
for i in {1..10}; do
  grpcurl -plaintext \
    -proto examples/proto/test_service.proto \
    -H "Host: grpc.example.com" \
    -d '{"name":"LoadTest"}' \
    127.0.0.1:18080 \
    test.TestService/SayHello 2>&1 | grep serverAddr
done

# 7. 查看 gRPC 访问日志
tail -f logs/edgion_access.log | grep -i grpc

# 8. 自动化测试脚本
./tests/test_grpc_routes.sh